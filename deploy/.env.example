# Copy to deploy/.env and adjust before a real deployment.
# deploy/.env is gitignored — never commit real credentials.

# --- mysql ---
MYSQL_ROOT_PASSWORD=root123
MYSQL_DATABASE=apartmaster
MYSQL_USER=apartmaster
MYSQL_PASSWORD=apartmaster123

# --- backend ---
JWT_SECRET=apartmaster_secret_key_2024_dev_only

# --- host ports (change if any of these are already taken on your machine) ---
FE_PORT=8080
BE_PORT=3001
MYSQL_PORT=3306
PHPMYADMIN_PORT=8081
